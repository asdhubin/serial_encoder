<Odometer.h>
  定义了一个类，成员是千米、米、毫米，只是为了输出距离时显得不那么冗长。
  成员函数是odo_print(),odo_print_positive(),odo_print_negative(),odo_add_mm(),odo_add_m().
  对应是   前进和后退距离    前进距离           后退距离          加毫米        加米
.cpp
  首先初始化串口，初始化转速——速度比，编码器转化到速度要按车轮比例的，你懂的。
  后面是读取数据，这里忽略了校验和。
